#ifndef QUESTION
#define QUESTION_H

typedef struct question{
  char question[256];
  char answer1[256];
  char answer2[256];
  char answer3[256];
  char answer4[256];
  int number_answers;
}question;

#endif
